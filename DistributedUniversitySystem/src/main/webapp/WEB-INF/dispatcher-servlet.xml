<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Component Scanning for Controllers, Services, and Repositories -->
    <context:component-scan base-package="com.university" />

    <!-- MVC Annotation-driven Configuration -->
    <mvc:annotation-driven />

    <!-- Default Servlet Handler for Static Resources -->
    <mvc:default-servlet-handler />

    <!-- Static Resources Mapping (CSS, JavaScript, Images) -->
    <mvc:resources mapping="/resources/**" location="/resources/" />

    <!-- View Resolver Configuration for JSP Pages -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
        <property name="order" value="1" />
    </bean>

    <!-- ========== DATABASE CONFIGURATION ========== -->

    <!-- PostgreSQL DataSource with Connection Pooling using Apache Commons DBCP2 -->
    <bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource">
        <property name="driverClassName" value="org.postgresql.Driver" />
        <property name="url" value="jdbc:postgresql://localhost:5437/DistributedUniversitySystem" />
        <property name="username" value="jackson" />
        <property name="password" value="jackson123" />
        <!-- Connection Pool Configuration -->
        <property name="initialSize" value="5" />
        <property name="maxTotal" value="20" />
        <property name="maxIdle" value="10" />
        <property name="maxWaitMillis" value="30000" />
        <property name="validationQuery" value="SELECT 1" />
        <property name="testOnBorrow" value="true" />
    </bean>

    <!-- JdbcTemplate Bean for Data Access Operations -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <constructor-arg ref="dataSource" />
    </bean>

</beans>